version: 2

sources:

  - name: alumno25_users # name of the source 
    schema: alumno25_users # this is the schema our raw data lives in
    database: ALUMNO25_DEV_BRONZE_DB # this is the name of our database

    tables:

      - name: user
        columns:
          - name: Nombre
          - name: DNI
          - name: email
          - name: fecha_alta_sistema